import OnboardingPhoto from "@/components/onboarding/OnboardingPhoto";

export const metadata = {
  title: "Stylé — Add Your Photo",
};

export default function PhotoPage() {
  return <OnboardingPhoto />;
}
